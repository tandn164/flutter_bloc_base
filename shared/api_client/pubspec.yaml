name: api_client
description: REST transport primitives and safe Chopper response decoding.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  chopper: ^8.1.0
  http: ^1.1.0
  app_result:
    path: ../app_result

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
