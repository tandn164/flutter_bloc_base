name: announcements_presentation
description: Flutter UI for announcements.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
  flutter: ">=3.16.0"
resolution: workspace

dependencies:
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.1.0
  injectable: ^2.5.0
  get_it: ^8.0.3
  flutter:
    sdk: flutter
  announcements_domain:
    path: ../announcements_domain

dev_dependencies:
  build_runner: ^2.4.13
  freezed: 3.0.6
  injectable_generator: ^2.7.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
