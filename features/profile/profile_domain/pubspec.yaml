name: profile_domain
description: Profile domain — UserProfile, repository, use cases.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  injectable: ^2.5.0
  get_it: ^8.0.3
  app_result:
    path: ../../../shared/app_result

dev_dependencies:
  build_runner: ^2.4.13
  injectable_generator: ^2.7.0
  lints: ^6.0.0
  test: ^1.25.0
