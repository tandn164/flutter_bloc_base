import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:injectable/injectable.dart';
import 'package:__name___domain/__name___domain.dart';

import '__name___event.dart';
import '__name___state.dart';

@injectable
class __Pascal__Bloc extends Bloc<__Pascal__Event, __Pascal__State> {
  __Pascal__Bloc(this._listItems) : super(const __Pascal__Loading()) {
    on<__Pascal__Started>((event, emit) async {
      emit(const __Pascal__Loading());
      try {
        final items = await _listItems();
        if (!emit.isDone) emit(__Pascal__Data(items));
      } catch (_) {
        if (!emit.isDone) emit(const __Pascal__Error('Unable to load items'));
      }
    });
  }

  final List__Pascal__Items _listItems;
}
